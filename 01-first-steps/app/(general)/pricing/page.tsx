import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Pricing",
    description: "Pricing information",
};

export default function PricingPage() {
    return ( <span className="text-6xl">Pricing</span> );
}